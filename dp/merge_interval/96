package main

import "fmt"

func numTrees(n int) int {
	dp := make([][]int, n+1)
	for i := 0; i < len(dp); i++ {
		dp[i] = make([]int, n+1)
	}
	for i:=1;i<=n;i++{
		dp[i][i]=1
	}


	for l:=1;l<=n;i++{
		for i:=1;i<=n-l+1;i++{
		}
		for root:=1;root<=n;root++{
			for l := max(0,nodes-root);l<=min(n,nodes+root);l++{
				dp[l][l+nodes-1]+=
			}
		}
		dp[]
	}
}
func main() {
	fmt.Println(numTrees(3))
}
